export interface RegisWard {
    ward_id: number;
    ward_name: string;
}

export interface RegisDistrict {
    district_id: number;
    district_name: string;
}

export interface RegisProvince {
    province_id: number;
    province_name: string;
}
